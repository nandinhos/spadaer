# Feature: Ajuste de Roteamento e Conflitos

> **ID:** 4.2
> **Agente Responsável:** Backend
> **Status:** ✅ Concluído

## 🎯 Objetivo
Corrigir o conflito de rotas no módulo de Documentos, onde wildcards (`{document}`) estão capturando rotas estáticas (`/create`, `/export`, `/import`), causando erros 404 inesperados.

## 🛠️ Plano de Ação (Ciclo TDD)

### Passo 1: Red (Falha)
- Criar um teste de funcionalidade (`tests/Feature/RouteConflictTest.php`) que tente acessar:
  - `GET /documents/create`
  - `GET /documents/export`
  - `POST /documents/import`
- Atualmente, espera-se que esses testes falhem ou retornem 404 devido ao conflito identificado.

### Passo 2: Green (Correção)
- Reorganizar o arquivo `routes/web.php`.
- Mover as rotas estáticas (`/create`, `/export`, `/import`) para **antes** das rotas com parâmetros (`/{document}`).
- Validar se o `Route Model Binding` não está mais tentando resolver "create" como um ID.

### Passo 3: Refactor
- Agrupar rotas de forma mais limpa se necessário.
- Verificar se outras entidades (`Boxes`, `Projects`) possuem conflitos similares.

## 🧪 Critérios de Aceite
- [ ] Acesso ao formulário de criação de documento (`documents.create`) retorna status 200.
- [ ] Acesso à exportação (`documents.export`) inicia o download ou retorna 200/302.
- [ ] Visualização de um documento real (`documents.show`) continua funcionando normalmente.

---
**Data de Criação:** 2026-02-06
