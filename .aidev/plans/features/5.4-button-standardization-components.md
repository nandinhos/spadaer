# Feature: Padronizacao de Botoes — Componentes Reutilizaveis

> **ID:** 5.4
> **Sprint:** 5
> **Agente Responsavel:** Frontend
> **Status:** 🟡 Pendente
> **Prioridade:** 🟢 MEDIA

## 🎯 Objetivo
Padronizar botoes nos componentes Blade compartilhados (modal, filtros, tabela de documentos).

## 📋 Escopo

### 5.4.1 — components/document-modal.blade.php (2 botoes)
- **Arquivo:** `resources/views/components/document-modal.blade.php`
- `<x-secondary-button>Fechar` → `<x-ui.button variant="secondary">Fechar`
- `<x-primary-button>Editar` → `<x-ui.button variant="primary" icon="fas fa-edit">Editar`

### 5.4.2 — components/document-filters.blade.php (2 botoes)
- **Arquivo:** `resources/views/components/document-filters.blade.php`
- `<button class="...">Limpar` (HTML inline) → `<x-ui.button variant="ghost" size="sm">Limpar`
- `<x-primary-button>Filtrar` → `<x-ui.button variant="primary" size="sm">Filtrar`

### 5.4.3 — components/document-table.blade.php (4 botoes)
- **Arquivo:** `resources/views/components/document-table.blade.php`
- `<x-primary-button>Novo Documento` → `<x-ui.button variant="primary" size="sm" icon="fas fa-plus">Novo`
- `<x-secondary-button>Exportar` → `<x-ui.button variant="success" size="sm" icon="fas fa-file-export">Exportar`
- Acoes tabela editar → `ghost-warning` size="sm"
- Acoes tabela ver → `ghost-primary` size="sm"

## ⚠️ Atencao na Implementacao
1. Preservar `x-on:click="$store.modals.closeDocumentDetails()"` no modal
2. Preservar `x-show="$store.modals.selectedDocument?.id"` no botao editar do modal
3. Componentes afetam TODAS as paginas que os usam — testar amplamente

## 🧪 Criterios de Aceite
- [ ] Componentes compartilhados usam `<x-ui.button>`
- [ ] Todas as paginas que incluem esses componentes renderizam OK
- [ ] Alpine.js bindings preservados
- [ ] Dark mode OK

---
**Data de Criacao:** 2026-02-07
